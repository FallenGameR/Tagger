<Window x:Class="Tagger.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:markupExtensions="clr-namespace:Utils.Prism;assembly=Tagger.Lib"
        xmlns:views="clr-namespace:Tagger.Wpf.Views"
        Background="#FAFAFA"
        Title="MainWindow" Width="475"
        SizeToContent="Height"
        FocusManager.FocusedElement="{markupExtensions:FirstFocusedElement}"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <views:HookControl Grid.Column="0" Grid.Row="0" Height="172"/>
        <views:HotkeyControl Grid.Column="0" Grid.Row="1" Height="80" />
    </Grid>
</Window>
